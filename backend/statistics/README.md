# Statistics Service

Python gRPC service backed by ClickHouse with Kafka for event streaming.

## Notes

- gRPC definitions live in `proto/statistics.proto`.
- Implement ClickHouse queries in `src/`.
