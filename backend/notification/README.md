# Notification Service

Python gRPC notification service backed by MongoDB with Kafka for events.

## Notes

- gRPC definitions live in `proto/notification.proto`.
- Implement messaging handlers in `src/`.
