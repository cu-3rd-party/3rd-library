# Engagement Service

Python gRPC engagement service (comments/likes) backed by MongoDB with Kafka.

## Notes

- gRPC definitions live in `proto/engagement.proto`.
- Implement engagement logic in `src/`.
